# Heatmap generator

A simple heatmap maker for .fit files (I might add .gpx support too, eventually). You drag and drop (or select) some files, and get an interactive heatmap!

I made this because I couldn't be arsed to pay for a Strava subscription, but I think the heatmap is cool :)
